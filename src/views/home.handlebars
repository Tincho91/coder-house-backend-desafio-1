<div>
    <h1>Hola {{mensaje}}</h1>
    {{> itemCounter}}
   <p>Hola, {{user.nombre}}</p>
   {{#if isAdmin}}
        {{#each cursos}}
            <div>
                <p>{{this.numComision}}</p>
                <p>{{this.dias}}</p>
                <p>{{this.horario}}</p>
            </div>
        {{/each}}
   {{else}}
    <p>No sos Admin</p>
   {{/if}}
</div>